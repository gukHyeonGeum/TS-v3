import { ChangeStarPipe } from './change-star.pipe';

describe('ChangeStarPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeStarPipe();
    expect(pipe).toBeTruthy();
  });
});
