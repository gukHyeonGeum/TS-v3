import { SocialNamePipe } from './social-name.pipe';

describe('SocialNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SocialNamePipe();
    expect(pipe).toBeTruthy();
  });
});
